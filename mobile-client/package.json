{
  "name": "mobile-client",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "flutter run",
    "build:apk": "flutter build apk",
    "build:ios": "flutter build ios",
    "test": "flutter test",
    "lint": "flutter analyze"
  }
}
